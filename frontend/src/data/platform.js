/**
 * This file is intentionally empty.
 *
 * All mock data has been removed. UI components now fetch real data
 * from the backend REST API and WebSocket.
 *
 * See:
 *   src/api/        — HTTP API client functions
 *   src/hooks/      — React hooks that call the API
 */
